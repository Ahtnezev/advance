<meta charset="utf-8">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <meta name="author" content="Iren Etneciv">
        <meta name="theme-color" content="{{ config('recycle.web.metas.theme_color') }}" />
        <meta name="description" content="{{ config('recycle.web.metas.description') }}">
        <meta name="keywords" content="{{ config('recycle.web.metas.keywords') }}">
        <title>{{ env('APP_NAME', 'Advance Detail') }}</title>
        <!-- Fonts -->
        <link rel="preconnect" href="https://fonts.googleapis.com">
        <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
        <link
            href="https://fonts.googleapis.com/css2?family=Audiowide&family=Montserrat:ital,wght@0,100..900;1,100..900&display=swap"
            rel="stylesheet">
        <!-- Styles Map -->
        <link rel="stylesheet" href="{{ asset('css/leaflet.css') }}">
        <!-- Styles -->
        <link href="{{ asset('css/bootstrap.min.css') }}" rel="stylesheet">
